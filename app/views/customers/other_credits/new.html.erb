<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <div class="box box-default">
      <div class="box-header bg-gray">
        <center>
          <%= image_tag(@customer.avatar.url(:medium), class: "img-circle",height: 60, width: 60) %>
          <h4><%= @customer.name %></h4>
        </center>
      </div>
      <div class="box-body">
        <%= simple_form_for @other_credit, url: :customer_other_credits do |f| %>
          <%= f.input :date, as: :string, input_html: { class: "datepicker" } %>
          <%= f.input :amount %>
          <%= f.input :description %>
          <%= f.input :reference_number %>
          <%= f.input :employee_id, input_html: { value: current_user.id }, as: :hidden %>
          <%= f.input :customer_id, input_html: { value: @customer.id }, as: :hidden %>
          <div class="box-footer">
            <%= f.submit "Save Other Credit", class: "btn btn-primary btn-block" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
