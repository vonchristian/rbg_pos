<h3> Settings</h3>
<%= link_to 'New Department', new_customer_department_path(@customer), class: 'btn btn-warning' %>
<% @customer.departments.each do |department| %>
  <%= department.name %>
  <br>
<% end %>

 <%= link_to 'Edit Customer', edit_customer_path(@customer), class: "btn btn-warning btn-sm" %>
    <% if current_user.proprietor? %>
      <%= link_to 'Delete Customer', customer_path(@customer), class: "btn btn-warning btn-sm", method: :delete, data: { confirm_with: "Are you sure you want to delete this customer?" } %>
    <% end %>
